#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * Strips the carriage return from the String passed in
 */
void stripCarriageReturn(char * array);


#endif
